import { identities } from "./identities.js";
import { authorityEvents } from "./authority-events.js";
import { policyRules } from "./policy-rules.js";
import { decisionRecords } from "./decision-records.js";

export const schema = {
  identities,
  authorityEvents,
  policyRules,
  decisionRecords,
};
